% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_scfa.R
\name{run_scfa}
\alias{run_scfa}
\title{Run the full Sequential Confirmatory Factor Analysis workflow}
\usage{
run_scfa(
  stage_models,
  data,
  method = c("bartlett", "regression"),
  threshold = 0.7,
  correct = TRUE,
  cfa_args = list()
)
}
\arguments{
\item{stage_models}{A non-empty character vector or list of character
strings, each a valid \strong{lavaan} model syntax string.  Element \code{s}
specifies the CFA model for stage \code{s}.  The first element is fitted to
\code{data}; subsequent elements are fitted to the factor scores from the
previous stage.}

\item{data}{A \code{data.frame} (or coercible object) containing the
observed indicators for the first stage.}

\item{method}{Character scalar: the factor-score extraction method.
One of \code{"bartlett"} (default) or \code{"regression"}.}

\item{threshold}{Numeric scalar in (0, 1).  Reliability threshold for
\code{\link{scfa_propagation_diagnostics}}.  Default \code{0.70}.}

\item{correct}{Logical.  If \code{TRUE} (default), apply the appropriate
post-hoc correction at the final stage.}

\item{cfa_args}{A named list of additional arguments forwarded to
\code{lavaan::cfa()} at every stage.  Default \code{list()}.}
}
\value{
A named list of class \code{"scfa_result"} with elements:
\describe{
  \item{\code{fits}}{Named list of fitted \code{lavaan} objects.}
  \item{\code{scores}}{Named list of \code{data.frame}s of factor scores.}
  \item{\code{diagnostics}}{Named list of \code{scfa_diagnostics} tables.}
  \item{\code{propagation_chain}}{Cumulative propagation variances per stage.}
  \item{\code{index_scores}}{Final-stage composite index scores.}
  \item{\code{correction}}{Post-hoc correction applied to the final stage, or \code{NULL}.}
  \item{\code{method}}{The \code{method} argument as supplied.}
  \item{\code{threshold}}{The \code{threshold} argument as supplied.}
}
}
\description{
Orchestrates the complete Sequential CFA pipeline: fits each stage of a
hierarchical CFA model in turn, extracts factor scores to pass forward,
computes error-propagation diagnostics at every stage, optionally applies
the appropriate post-hoc correction, and returns a structured result list.
}
\examples{
\dontrun{
library(lavaan)
set.seed(42)
pop <- '
  subfactor1 =~ 0.7*x1 + 0.7*x2 + 0.7*x3
  subfactor2 =~ 0.8*x4 + 0.8*x5 + 0.8*x6
  subfactor1 ~~ 1*subfactor1; subfactor2 ~~ 1*subfactor2
  subfactor1 ~~ 0*subfactor2
'
dat <- lavaan::simulateData(pop, sample.nobs = 300)

result <- run_scfa(
  stage_models = list(
    "subfactor1 =~ x1 + x2 + x3\nsubfactor2 =~ x4 + x5 + x6",
    "index =~ subfactor1 + subfactor2"
  ),
  data      = dat,
  method    = "bartlett",
  threshold = 0.70
)
print(result)
head(result$index_scores)
}
}
\seealso{
\code{\link{scfa_propagation_diagnostics}},
\code{\link{scfa_correct_loadings}},
\code{\link{scfa_correct_residuals}},
\code{\link{scfa_propagate_chain}}
}
