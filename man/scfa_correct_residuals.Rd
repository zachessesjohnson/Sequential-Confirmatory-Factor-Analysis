% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scfa_propagation.R
\name{scfa_correct_residuals}
\alias{scfa_correct_residuals}
\title{Correct Stage-2 residual variances for Bartlett-score propagation error}
\usage{
scfa_correct_residuals(fit2, fit1)
}
\arguments{
\item{fit2}{A fitted \code{lavaan} CFA object for the Stage-2 model.
Its observed variables must be the Stage-1 Bartlett factor scores, one per
first-order factor of \code{fit1} (in the same order).}

\item{fit1}{A fitted \code{lavaan} CFA object for the Stage-1 model.
Used to compute \eqn{\hat\psi_{\nu_1,k}} via
\code{\link{scfa_propagation_variance}}.}
}
\value{
A numeric vector of length equal to the number of Stage-1 factors,
giving the adjusted Stage-2 residual variances
\eqn{\hat\theta_{2,k}^{\text{adj}}}.  Entries that would become negative
after adjustment are clamped to zero with a warning.
}
\description{
When \strong{Bartlett} factor scores are used as Stage-2 inputs, the
Stage-2 loading estimates are asymptotically unbiased, but each Stage-2
residual variance is inflated by the propagated estimation-error variance
of the corresponding Stage-1 factor:
\deqn{\hat\theta_{2,k}^{\text{adj}} = \hat\theta_{2,k} - \hat\psi_{\nu_1,k},}
where \eqn{\hat\psi_{\nu_1,k} = 1/\hat{I}_k}.
}
\details{
This correction is intended for \strong{Bartlett} factor scores only.
Under regression scores use \code{\link{scfa_correct_loadings}} instead.
}
\examples{
\dontrun{
scores_bart <- as.data.frame(lavPredict(fit_stage1, method = "bartlett"))
fit_stage2  <- cfa(model_stage2, data = scores_bart)
adj_theta   <- scfa_correct_residuals(fit_stage2, fit_stage1)
}
}
\seealso{
\code{\link{scfa_correct_loadings}}, \code{\link{scfa_propagation_variance}}
}
