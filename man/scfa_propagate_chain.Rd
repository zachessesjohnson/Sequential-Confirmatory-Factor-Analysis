% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scfa_propagation.R
\name{scfa_propagate_chain}
\alias{scfa_propagate_chain}
\title{Accumulate propagation variances across a multi-stage sequential CFA chain}
\usage{
scfa_propagate_chain(fits)
}
\arguments{
\item{fits}{A named or unnamed list of fitted \code{lavaan} CFA objects,
one per stage, ordered from lowest (Stage 1) to highest (Stage S) level.}
}
\value{
A list with one element per stage, each being a named numeric vector
of cumulative propagation variances.  The final element therefore contains
the total accumulated propagation variance entering the final stage's
inputs.
}
\description{
In a hierarchy with more than two stages, estimation error accumulates at
each stage.  Given a list of fitted lavaan objects, this function sums the
propagated error variances stage by stage:
\deqn{\Psi_{\nu,k}^{(s)} = \sum_{t=1}^{s} \psi_{\nu,k}^{(t)},}
where \eqn{\psi_{\nu,k}^{(t)} = 1/I_k^{(t)}}.
}
\examples{
\dontrun{
chain <- scfa_propagate_chain(list(fit_stage1, fit_stage2))
# Total accumulated propagation variance after Stage 2:
chain[[2]]
}
}
\seealso{
\code{\link{scfa_propagation_variance}}
}
